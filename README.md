# Recipe Manager (HTML / CSS / JavaScript / localStorage)

## Overview
Single-page Recipe Manager for assessment. Uses browser `localStorage` (key: `recipes`) to persist data. Supports create, read, update, delete, search, and filter.


